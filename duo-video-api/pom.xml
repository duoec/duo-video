<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
    <modelVersion>4.0.0</modelVersion>
    <parent>
        <groupId>com.duoec.video</groupId>
        <artifactId>duo-video</artifactId>
        <version>1.0.0-SNAPSHOT</version>
    </parent>

    <artifactId>duo-video-api</artifactId>

    <properties>
        <spring-boot.version>3.5.8</spring-boot.version>
        <maven.compiler.source>21</maven.compiler.source>
        <maven.compiler.target>21</maven.compiler.target>
        <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    </properties>

    <profiles>
        <profile>
            <id>prod</id>
            <properties>
                <duo-video-jy.version>1.0.0</duo-video-jy.version>
            </properties>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
        </profile>
        <profile>
            <id>dev</id>
            <properties>
                <duo-video-jy.version>1.0.0-SNAPSHOT</duo-video-jy.version>
            </properties>
            <activation>
                <activeByDefault>true</activeByDefault>
            </activation>
        </profile>
    </profiles>

    <dependencyManagement>
        <dependencies>
            <!-- 导入 Spring Boot 的依赖管理 -->
            <dependency>
                <groupId>org.springframework.boot</groupId>
                <artifactId>spring-boot-dependencies</artifactId>
                <version>${spring-boot.version}</version>
                <type>pom</type>
                <scope>import</scope>
            </dependency>
        </dependencies>
    </dependencyManagement>

    <dependencies>
        <dependency>
            <groupId>com.duoec.video</groupId>
            <artifactId>duo-video-jy</artifactId>
            <version>${duo-video-jy.version}</version>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter</artifactId>
        </dependency>

        <dependency>
            <groupId>org.springframework.boot</groupId>
            <artifactId>spring-boot-starter-web</artifactId>
        </dependency>
    </dependencies>

    <build>
        <plugins>

            <!-- 重要！Duo-Doc，通过解析源码、注释自动生成接口信息 -->
            <plugin>
                <groupId>org.apache.maven.plugins</groupId>
                <artifactId>maven-javadoc-plugin</artifactId>
                <version>3.3.1</version>
                <configuration>
                    <doclet>com.duoec.doc.DuoDoclet</doclet>
                    <docletArtifact>
                        <groupId>com.duoec</groupId>
                        <artifactId>duo-doclet-17</artifactId>
                        <version>1.0.7</version>
                    </docletArtifact>
                    <useStandardDocletOptions>false</useStandardDocletOptions>

                    <includeDependencySources>true</includeDependencySources>
                    <includeTransitiveDependencySources>true</includeTransitiveDependencySources>
                    <dependencySourceIncludes>
                        <dependencySourceInclude>com.duoec*:*</dependencySourceInclude>
                    </dependencySourceIncludes>

                    <additionalOptions>
                        <additionalOption>-doc.basePath ${project.basedir}</additionalOption>
                        <additionalOption>-doc.appId duo-video-api</additionalOption>
                        <additionalOption>-doc.exporter open-api</additionalOption>
                        <additionalOption>-doc.exporter.openApiServer file://${project.basedir}/open-api.json</additionalOption>
                    </additionalOptions>
                </configuration>
                <executions>
                    <execution>
                        <id>attach-javadocs</id>
                        <!-- package可以在提交代码后由CI自动触发，如果不需要自动触发，可以设置为site，届时需要手工执行：mvn clean site -->
                        <phase>compile</phase>
                        <goals>
                            <goal>javadoc</goal>
                        </goals>
                    </execution>
                </executions>
            </plugin>

        </plugins>
    </build>
</project>